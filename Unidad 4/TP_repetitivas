#Ej1 Crea un programa que imprima en pantalla todos los números enteros desde 0 hasta 100 (incluyendo ambos extremos)
#en orden creciente, mostrando un número por línea. 

for i in range(101):  # El rango va de 0 a 100
    print(i)

#Ej2 Desarrolla un programa que solicite al usuario un número entero
# y determine la cantidad de dígitos que contiene

#pedimos al usuario un número
num = int(input("Ingresa un número entero: "))
#usamos un bucle while  para dividir el número por 10 
#hasta que sea 0, contando cada división.
if num == 0:
    print("El número tiene 1 dígito.")
else:
    count = 0
    num_abs = abs(num) # Usamos el valor absoluto para manejar números negativos
    while num_abs > 0:
        num_abs //= 10
        count += 1
    print(f"El número tiene {count} dígitos.")

#Ej3 Escribe un programa que sume todos los números enteros comprendidos 
#entre dos valores dados por el usuario, excluyendo esos dos valores

#pedimos los numeros al usuario
valor1 = int(input("Ingresa el primer valor entero: "))
valor2 = int(input("Ingresa el segundo valor entero: "))
suma = 0

# Intercambiar valores si el primero es mayor que el segundo
if valor1 > valor2:
    valor1, valor2 = valor2, valor1

# El rango excluye el valor final, por eso no sumará 'valor2'
for i in range(valor1 + 1, valor2):
    suma += i
    
print(f"La suma de los números entre {valor1} y {valor2} es: {suma}")

#Ej4 Elabora un programa que permita al usuario ingresar números enteros 
#y los sume en secuencia. El programa debe detenerse 
#y mostrar el total acumulado cuando el usuario ingrese un 0. 
#usamos while para que continúe hasta que 
#la condición de salida (ingresar 0) se cumple
total = 0
while True:
    num = int(input("Ingresa un número (o 0 para detener): "))
    if num == 0:
        break
    total += num
print(f"El total acumulado es: {total}")

#Ej5 Crea un juego en el que el usuario deba adivinar un número aleatorio entre 0 y 9. 
#Al final, el programa debe mostrar cuántos intentos fueron necesarios 
#para acertar el número. 

import random

numero_secreto = random.randint(0, 9)
intentos = 0
adivinado = False

while not adivinado:
    intento_usuario = int(input("Adivina el número entre 0 y 9: "))
    intentos += 1
    if intento_usuario == numero_secreto:
        adivinado = True
        print(f"¡Felicitaciones! Adivinaste el número en {intentos} intentos.")
    elif intento_usuario < numero_secreto:
        print("El número es más alto. ¡Intenta de nuevo!")
    else:
        print("El número es más bajo. ¡Intenta de nuevo!")

#Ej6 Desarrolla un programa que imprima en pantalla todos los números pares 
#comprendidos entre 0 y 100, en orden decreciente. 
#for con un paso negativo es la solución más directa.

for i in range(100, -1, -2):
    print(i)

#-------------------------------------------------------------------------------
#Ej7 Crea un programa que calcule la suma de todos los números 
#comprendidos entre 0 y un número entero positivo indicado por el usuario.
#pedimos al usuario un numero positivo
num_limite = int(input("Ingresa un número entero positivo: "))

#for  para sumar los números en el rango especificado
if num_limite >= 0:
    suma = 0
    for i in range(num_limite + 1):
        suma += i
    print(f"La suma de los números desde 0 hasta {num_limite} es: {suma}")
else:
    print("El número debe ser positivo.")

#Ej8 Escribe un programa que permita al usuario ingresar 100 números enteros.
#Luego, el programa debe indicar cuántos de estos números son pares,
#cuántos son impares, cuántos son negativos y cuántos son positivos. 
#(Nota: para probar el programa puedes usar una cantidad menor,
#pero debe estar preparado para procesar 100 números con un solo cambio). 

cantidad_numeros = 100  # Se puede cambiar este valor para probar

pares = 0
impares = 0
positivos = 0
negativos = 0

for i in range(cantidad_numeros):
    num = int(input(f"Ingresa el número {i + 1}: "))
    
    # Contar pares e impares
    if num % 2 == 0:
        pares += 1
    else:
        impares += 1
        
    # Contar positivos y negativos
    if num > 0:
        positivos += 1
    elif num < 0:
        negativos += 1

print("\n--- Resultados ---")
print(f"Números pares: {pares}")
print(f"Números impares: {impares}")
print(f"Números positivos: {positivos}")
print(f"Números negativos: {negativos}")

#Ej9 Elabora un programa que permita al usuario ingresar 100 números enteros
#y luego calcule la media de esos valores.
#(Nota: puedes probar el programa con una cantidad menor, 
#pero debe poder procesar 100 números cambiando solo un valor). 

cantidad_numeros = 100  # Se puede cambiar este valor para probar
suma = 0

for i in range(cantidad_numeros):
    num = int(input(f"Ingresa el número {i + 1}: "))
    suma += num

media = suma / cantidad_numeros
print(f"\nLa media de los {cantidad_numeros} números es: {media}")

#Ej10 Escribe un programa que invierta el orden de los dígitos 
#de un número ingresado por el usuario. 
#Ejemplo: si el usuario ingresa 547, el programa debe mostrar 745

#pedimos al usuario un múmero
num = int(input("Ingresa un número: "))
num_invertido = 0

while num > 0:
    digito = num % 10
    num_invertido = (num_invertido * 10) + digito
    num //= 10
#imprimos el número invertido
print(f"El número invertido es: {num_invertido}")